{
  // 应用名称
  "name": "travel-vue",
  // 静态资源配置
  "assets": {
    // 构建产物所在目录（Vite 默认输出到 dist）
    "baseDirectory": "dist"
  }
  // 如果后续需要云函数，可添加以下配置：
  // "function": {
  //   "sourceFile": "api/index.js",
  //   "handler": "handler"
  // }
}
